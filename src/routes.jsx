import React from 'react'
import { Switch, Route } from 'react-router-dom'


export default (
  <Switch>
    <Route component={} exact path='/' />
    <Route component={} path='/add' />
    <Route component={} path='/edit/:id' />
  </Switch>
)